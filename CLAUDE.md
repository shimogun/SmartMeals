# SmartMeals プロジェクト定義 & 憲法

## 1. プロジェクト概要
- **名称:** SmartMeals (スマートミールズ)
- **目的:** 糖尿病と診断を受けたユーザーの健康と好みに最適化された食事管理・提案プラットフォーム。
- **プラットフォーム:** Android (Expo Go) 最優先。

## 2. 基本方針 (Core Principles)
- **最小限の変更:** 指示された箇所以外の既存コードを書き換えない。リファクタリングは明示的な指示がある場合のみ行う。
- **逐次実行と確認:** 複数の工程が必要な場合、一度にすべて提案せず、1工程ずつ実行し、ユーザーの確認（実機テスト）を待つ。
- **Androidファースト:** UIや挙動はAndroid実機（Expo Go）での動作を最優先し、iOS向けの調整は後回しとする。

## 3. 技術・環境ルール
- **互換性優先のインストール:** パッケージ追加は必ず `npx expo install` を使用し、Expo SDKとの互換性を保つ。
- **型安全の維持:** TypeScriptを使用し、既存の型定義を壊さない。
- **GitHub管理:**
    - 原則として1日の作業終了時に、その日の変更内容を要約した日本語のメッセージと共に commit & push を提案すること。
    - 大規模な機能の完成時や指示があった際も都度提案する。
    - `.env` などの機密情報は絶対にリポジトリに含めない。
- **ログの取り扱い:** エラーログを解析する際は、チャット容量節約のため、必要な箇所のみを抽出して報告を求める。

## 4. 命名・品質ルール
- **命名:** 変数・関数はキャメルケース、コンポーネントはパスカルケース。
- **コミュニケーション:** 実装前にまず設計の意図をユーザーに確認し、合意を得てからコードを生成する。